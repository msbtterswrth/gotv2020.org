{% extends 'paragraph.html.twig' %}
{% block content %}
{% set container = paragraph.field_container_size.value %}
{% set layout = paragraph.field_layout.value %}
{% set gutter = paragraph.field_gutter_size.value %}
<div class="{{container}}">
  <div class="item-grid">
    <h2>{{ content.field_title.0 }}</h2>
    <div class="body-text">{{ content.field_blurb }}</div>
    <div class="clearfix grid {{gutter}} {{layout}}">
        {{content.field_items}}
    </div>
  </div>
</div>
{% endblock %}